from .. import loader

# meta developer: @Itachi_Uchiha_sss

@loader.tds
class Papichpack2(loader.Module):
    """Голосовые сообщения от Arthas"""

    strings = {"name": "Papichpack2"}

    async def тильтcmd(self, message):
        """| ДА БЛЯТЬ Я В ТИЛЬТЕ СУКА"""

        reply = await message.get_reply_message()
        await message.delete()
        await message.client.send_file(
            message.to_id,
            "https://t.me/gachi_mych/247",
            voice_note=True,
            reply_to=reply.id if reply else None,
        )
        return

    async def этамнеcmd(self, message):
        """| ЭТА МНЕ ЭТА МНЕ?"""

        reply = await message.get_reply_message()
        await message.delete()
        await message.client.send_file(
            message.to_id,
            "https://t.me/gachi_mych/248",
            voice_note=True,
            reply_to=reply.id if reply else None,
        )
        return

    async def моральcmd(self, message):
        """| ПЛЮС МОРАЛЬ"""

        reply = await message.get_reply_message()
        await message.delete()
        await message.client.send_file(
            message.to_id,
            "https://t.me/gachi_mych/249",
            voice_note=True,
            reply_to=reply.id if reply else None,
        )
        return

    async def челты2cmd(self, message):
        """| ЧЕЛ ТЫ ЧЕЛ ТЫ ЧЕЛ ТЫ"""

        reply = await message.get_reply_message()
        await message.delete()
        await message.client.send_file(
            message.to_id,
            "https://t.me/gachi_mych/250",
            voice_note=True,
            reply_to=reply.id if reply else None,
        )
        return

    async def соглыcmd(self, message):
        """| Тут соглы плюс плюс плюс"""

        reply = await message.get_reply_message()
        await message.delete()
        await message.client.send_file(
            message.to_id,
            "https://t.me/gachi_mych/251",
            voice_note=True,
            reply_to=reply.id if reply else None,
        )
        return

    async def плачьcmd(self, message):
        """| Узнаете как надо плакать"""

        reply = await message.get_reply_message()
        await message.delete()
        await message.client.send_file(
            message.to_id,
            "https://t.me/gachi_mych/252",
            voice_note=True,
            reply_to=reply.id if reply else None,
        )
        return

    async def яумныйcmd(self, message):
        """| Потому что я умный"""

        reply = await message.get_reply_message()
        await message.delete()
        await message.client.send_file(
            message.to_id,
            "https://t.me/gachi_mych/253",
            voice_note=True,
            reply_to=reply.id if reply else None,
        )
        return

    async def минуточкуcmd(self, message):
        """| Минуточку"""

        reply = await message.get_reply_message()
        await message.delete()
        await message.client.send_file(
            message.to_id,
            "https://t.me/gachi_mych/254",
            voice_note=True,
            reply_to=reply.id if reply else None,
        )
        return

    async def хулиcmd(self, message):
        """| ХУЛИ ТЫ НЕ ВЫБРАЛ ТО ЧТО Я ХОТЕЛ?"""

        reply = await message.get_reply_message()
        await message.delete()
        await message.client.send_file(
            message.to_id,
            "https://t.me/gachi_mych/255",
            voice_note=True,
            reply_to=reply.id if reply else None,
        )
        return

    async def словоcmd(self, message):
        """| Слово не литературное"""

        reply = await message.get_reply_message()
        await message.delete()
        await message.client.send_file(
            message.to_id,
            "https://t.me/gachi_mych/256",
            voice_note=True,
            reply_to=reply.id if reply else None,
        )
        return

    async def налогcmd(self, message):
        """| СВЕРХ НАЛОГ, ТЫ СВЕРХ ХУЕСОС"""

        reply = await message.get_reply_message()
        await message.delete()
        await message.client.send_file(
            message.to_id,
            "https://t.me/gachi_mych/257",
            voice_note=True,
            reply_to=reply.id if reply else None,
        )
        return

    async def дадая2cmd(self, message):
        """| Я вернулся из небытия"""

        reply = await message.get_reply_message()
        await message.delete()
        await message.client.send_file(
            message.to_id,
            "https://t.me/gachi_mych/258",
            voice_note=True,
            reply_to=reply.id if reply else None,
        )
        return

    async def непон2cmd(self, message):
        """| Нихуя не понял2"""

        reply = await message.get_reply_message()
        await message.delete()
        await message.client.send_file(
            message.to_id,
            "https://t.me/gachi_mych/259",
            voice_note=True,
            reply_to=reply.id if reply else None,
        )
        return

    async def опоздалcmd(self, message):
        """| Всем привет, опоздал немного"""

        reply = await message.get_reply_message()
        await message.delete()
        await message.client.send_file(
            message.to_id,
            "https://t.me/gachi_mych/260",
            voice_note=True,
            reply_to=reply.id if reply else None,
        )
        return

    async def смех4cmd(self, message):
        """| Смех"""

        reply = await message.get_reply_message()
        await message.delete()
        await message.client.send_file(
            message.to_id,
            "https://t.me/gachi_mych/261",
            voice_note=True,
            reply_to=reply.id if reply else None,
        )
        return

    async def крик2cmd(self, message):
        """| Крик папани"""

        reply = await message.get_reply_message()
        await message.delete()
        await message.client.send_file(
            message.to_id,
            "https://t.me/gachi_mych/262",
            voice_note=True,
            reply_to=reply.id if reply else None,
        )
        return

    async def ебанутыйcmd(self, message):
        """| Я ёбнутый просто наглухо"""

        reply = await message.get_reply_message()
        await message.delete()
        await message.client.send_file(
            message.to_id,
            "https://t.me/gachi_mych/263",
            voice_note=True,
            reply_to=reply.id if reply else None,
        )
        return